"""
Week B — Day 1: Global Feature Importance — and Why Yours Was Probably Biased
=============================================================================

Practice placeholder — pairs with `1.0.global-importance.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. RF on Titanic + one injected `random_id` column (n unique values): show impurity importance ranks it absurdly 
  2. Duplicate a strong feature (`fare_copy = fare`): watch the two methods split/steal credit differently.
  3. Compare permutation importance computed on train vs validation: explain every rank flip.
  4. Write `importance_report(model, X_val, y_val)` returning a tidy DataFrame of both methods — reusable for Day 5

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 1.0.global-importance.md")
