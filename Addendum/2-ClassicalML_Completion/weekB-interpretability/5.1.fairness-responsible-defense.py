"""
Week B — Day 5: Fairness, Responsible AI & the Upgraded Defense
===============================================================

Practice placeholder — pairs with `5.0.fairness-responsible-defense.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. `fairness_report(model, X, y, group_col)` → per-group selection rate, TPR, FPR + the three parity gaps.
  2. The proxy experiment (predict the sensitive feature from the rest; report AUC).
  3. SHAP beeswarm sliced by group: does the model use different features for different groups?
  4. The model card (markdown, ~1 page) + the defense addendum. These two documents are the day's real output.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 5.0.fairness-responsible-defense.md")
