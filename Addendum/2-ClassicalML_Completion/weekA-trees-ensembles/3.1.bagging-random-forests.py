"""
Week A — Day 3: Bagging & Random Forests — Averaging Away Variance
==================================================================

Practice placeholder — pairs with `3.0.bagging-random-forests.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Implement bagging by hand: 50 deep DecisionTreeClassifiers on bootstraps + majority vote; compare
  2. Empirically verify ~36.8% OOB fraction for several n.
  3. Sweep max_features ∈ {1, √p, p/2, p}: plot CV mean ± std — find the decorrelation sweet spot.
  4. Plot CV score vs n_estimators (10→500): observe the plateau, confirm "more trees never hurts accuracy, only ti
  5. Compare your hand-rolled bagger vs RandomForestClassifier (same depth settings): the residual gap IS the per-s

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 3.0.bagging-random-forests.md")
