"""
Week A — Day 1: Decision Trees — The Split Math
===============================================

Practice placeholder — pairs with `1.0.decision-tree-split-math.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. `gini(labels)` and `entropy(labels)` from scratch; verify the 50/50 max property by plotting impurity vs p.
  2. `best_split(X_col, y)`: exhaustive threshold scan returning (threshold, gain); test on a toy 1-feature dataset
  3. Compare your chosen split with `DecisionTreeClassifier(max_depth=1)` on the same data — they must agree.
  4. Show scale invariance empirically: identical tree structure before/after log-transforming a feature.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 1.0.decision-tree-split-math.md")
