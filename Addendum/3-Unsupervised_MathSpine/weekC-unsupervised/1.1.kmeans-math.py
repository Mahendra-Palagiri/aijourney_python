"""
Week C — Day 1: k-Means — Objective, Algorithm, Failure Modes
=============================================================

Practice placeholder — pairs with `1.0.kmeans-math.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Lloyd's algorithm from scratch (~30 lines NumPy); animate/snapshot iterations on `make_blobs`.
  2. Verify monotone inertia decrease per iteration (plot it).
  3. Bad-init demo: deliberately seed two centroids in one blob; show the stuck local optimum; then k-means++ rescu
  4. Failure gallery: run on `make_moons` and anisotropic blobs; plot the wrong-but-confident partitions.
  5. Match your implementation vs sklearn KMeans (same init): identical assignments.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 1.0.kmeans-math.md")
