"""
Week C — Day 5: PCA in Practice — Compression, Visualization, Pipelines
=======================================================================

Practice placeholder — pairs with `5.0.pca-practice-pipelines.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Scatter Titanic in PC1–PC2 colored by survival; loadings table → name PC1 and PC2 in plain English.
  2. Pipeline(StandardScaler → PCA(k) → LogisticRegression): CV over k ∈ {2,…,all}; plot score vs k; compare to no-
  3. Eigen-digits: PCA on sklearn `load_digits`; show top-16 components as 8×8 images; reconstruct one digit at k=5
  4. Construct the adversarial case: synthetic data where the label lives along the SMALLEST-variance direction;

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 5.0.pca-practice-pipelines.md")
