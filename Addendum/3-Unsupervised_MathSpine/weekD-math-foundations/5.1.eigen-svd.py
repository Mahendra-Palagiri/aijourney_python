"""
Week D — Day 5: Eigenvectors, Eigenvalues & SVD — Special Directions
====================================================================

Practice placeholder — pairs with `5.0.eigen-svd.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Eigen-hunt visually: transformation-grid plot for a symmetric A; overlay computed eigenvectors (np.linalg.eigh
  2. Power iteration from scratch (repeated A@v + normalize) → dominant eigenvector; compare to NumPy. (10 lines;
  3. SVD image compression: grayscale photo, reconstruct at k=5/20/50; plot quality vs storage — the tactile Eckart
  4. Re-derive Week-C PCA both routes on the same data: eigh(covariance) vs svd(X_centered); show identical compone
  5. Verify orthogonality: components @ components.T ≈ I.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 5.0.eigen-svd.md")
