"""
Week D — Day 4: Linear Algebra Core — Vectors, Matrices, the Geometric View
===========================================================================

Practice placeholder — pairs with `4.0.linear-algebra-core.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Transformation zoo: apply rotation/scaling/shear/projection matrices to a grid of points + the unit square;
  2. Cosine similarity function from scratch; test on word-count toy vectors ("doc about cats" vs "doc about dogs" 
  3. Verify: columns of A = images of basis vectors (compute A@e1, A@e2 explicitly).
  4. Rank experiments: np.linalg.matrix_rank on (a) random matrix, (b) one with a duplicated column, (c) Titanic
  5. Implement `nn.Linear` forward pass as X@W.T + b in NumPy; match PyTorch's output exactly (weights copied over)

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 4.0.linear-algebra-core.md")
