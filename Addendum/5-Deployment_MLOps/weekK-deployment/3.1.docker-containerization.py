"""
Week K — Day 3: Docker — Shipping the Whole Environment
=======================================================

Practice placeholder — pairs with `3.0.docker-containerization.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Dockerfile + build + the containerized smoke test (Day-2's load script against the container).
  2. The layer-cache experiment: touch app code → rebuild (fast); touch requirements → rebuild (slow); timings reco
  3. Image-size before/after table (≥30% reduction is the bar).
  4. compose.yaml bringing up api+db; one end-to-end request that hits both.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 3.0.docker-containerization.md")
