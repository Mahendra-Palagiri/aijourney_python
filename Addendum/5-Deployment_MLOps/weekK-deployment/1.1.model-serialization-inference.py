"""
Week K — Day 1: Model Serialization & the Inference Contract
============================================================

Practice placeholder — pairs with `1.0.model-serialization-inference.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. `save_artifact()/load_artifact()` with metadata sidecar; round-trip tests for both model families.
  2. The version-drift simulation: load with a tweaked metadata mismatch → loud warning (your code), not silent wro
  3. `predict.py` with pydantic input model + 6 boundary tests (valid, missing field, wrong dtype, out-of-range, em
  4. A CLI inference run: `python -m predict --json '{"age": 29, ...}'` (Week J-2's argparse, reused).

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 1.0.model-serialization-inference.md")
