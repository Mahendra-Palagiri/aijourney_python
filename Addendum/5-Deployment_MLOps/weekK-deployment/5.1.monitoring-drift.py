"""
Week K — Day 5: Monitoring & Drift — The Model Met Reality; Reality Moved
=========================================================================

Practice placeholder — pairs with `5.0.monitoring-drift.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. `drift_report(reference_df, current_df)`: PSI + KS per feature + prediction-distribution comparison → markdown
  2. The simulation: 3 shift types × the report — a 3×N caught/missed matrix with commentary.
  3. Logging middleware addition to Day-2's API: every request's features + prediction + latency to a log table (th
  4. The alert-threshold experiment: false-alarm rate on UNshifted data at various PSI cutoffs (multiple-comparison
  5. The retraining runbook (1 page, committed).

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 5.0.monitoring-drift.md")
