"""
Week J — Day 5: Git Habits, CI & Packaging — Automation Floor
=============================================================

Practice placeholder — pairs with `5.0.git-ci-packaging.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. `.pre-commit-config.yaml` + the cleanup commit it forces.
  2. `.github/workflows/ci.yml` for the Week-J package; a screenshot/log of the first green run committed as eviden
  3. The fresh-venv test: new venv → `pip install -r addendum-requirements.txt` → pytest green (reproducibility, PR
  4. A failing-CI demo: push a type error, watch CI catch it, fix (red/green at the infrastructure level).

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 5.0.git-ci-packaging.md")
