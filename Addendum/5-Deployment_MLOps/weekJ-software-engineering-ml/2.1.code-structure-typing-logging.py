"""
Week J — Day 2: Structure, Typing & Logging — From Scripts to a Package
=======================================================================

Practice placeholder — pairs with `2.0.code-structure-typing-logging.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Restructure the Week-8 capstone into this layout in the Addendum workspace (originals untouched) — installable
  2. mypy clean run (document the 3 most interesting errors it caught).
  3. The pydantic config with two deliberate bad-config tests (Day-1 pytest habits, already compounding).
  4. Log files from a full training run, committed as evidence.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 2.0.code-structure-typing-logging.md")
