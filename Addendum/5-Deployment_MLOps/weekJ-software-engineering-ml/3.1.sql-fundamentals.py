"""
Week J — Day 3: SQL Fundamentals — Where Data Actually Lives
============================================================

Practice placeholder — pairs with `3.0.sql-fundamentals.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Build `titanic.db` (sqlite3 + pandas .to_sql, split into passengers/tickets/survival tables — a real schema wi
  2. 15-query drill ladder (filtering → aggregation → 2-table JOINs → CTE), each verified against the pandas equiva
  3. The row-multiplication JOIN trap, deliberately triggered and explained (duplicate keys → cartesian surprise).
  4. NULL trap gallery: 5 queries that LOOK right and aren't, fixed.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 3.0.sql-fundamentals.md")
