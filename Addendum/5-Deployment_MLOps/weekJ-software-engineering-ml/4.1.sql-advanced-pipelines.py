"""
Week J — Day 4: Advanced SQL & the Feature Pipeline
===================================================

Practice placeholder — pairs with `4.0.sql-advanced-pipelines.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Window-function drill ladder on a synthetic transactions table (10 queries: ranks, lags, 7-day windows).
  2. The leakage-safe customer-features query (CTEs + windows) for a churn-style label; verified row-for-row agains
  3. EXPLAIN before/after an index on a deliberately slow JOIN (timings recorded).
  4. End-to-end: SQL features → LGBM (Module-2 muscle) → harness evaluation; the whole thing under one pytest.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 4.0.sql-advanced-pipelines.md")
