"""
Week L — Day 2: Model Registry & Lifecycle — From "best run" to "the model in prod"
===================================================================================

Practice placeholder — pairs with `2.0.model-registry-lifecycle.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Register v1 + v2 (a retrained variant) of the Week-K model with full lineage.
  2. `promote.py` with criteria; run it: v2 passes/fails on its merits (engineer one of each case).
  3. The API loading by alias; the promotion → new-version-served-without-redeploy demo, logged.
  4. The rollback drill: flip back, verify serving, time it (the number goes in the runbook).

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 2.0.model-registry-lifecycle.md")
