"""
Week L — Day 4: Pipelines & Orchestration — Retraining While You Sleep
======================================================================

Practice placeholder — pairs with `4.0.pipelines-orchestration.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. The minimal runner + the 8-node retraining DAG, run end-to-end manually.
  2. The scheduled run (cron/scheduler) + the morning-after artifacts committed (run log, new candidate, eval repor
  3. The same DAG in Prefect; a ½-page comparison verdict (when is the framework worth it: team size? node count? r
  4. The partial-failure drill: kill node 4 mid-run; resume; verify no duplicates/corruption (idempotency, proven a

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 4.0.pipelines-orchestration.md")
