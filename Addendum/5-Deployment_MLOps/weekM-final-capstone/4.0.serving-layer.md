# Week M — Day 4: Serving — The Model Meets Its Users
> Pairs with `4.1.serving-layer.py`

## 📚 Today
1. Pattern per charter (K-4 decision): API and/or batch — built on the Week-K template (artifact contract,
   pydantic I/O, health endpoint, version from registry alias, structured logs).
2. Containerized (K-3), composed with the data layer (K-3 compose), deployed as Week K-4 did (PaaS/VM/local
   as charter SLOs demand).
3. The test battery: TestClient suite (J-1/K-2), boundary cases from the data contract (one contract, both
   sides — L-3), smoke load vs the latency SLO (K-2/K-6) — SLO met or the gap documented with a plan.
4. Failure drill, capstone edition (K-6): db down, malformed input, wrong-version artifact, model file
   missing — caught/logged/clean-error matrix.
5. Shadow-mode hook (K-4's design): the API can log a challenger's predictions alongside — Day 6 will use it.

## 🛠️ Deliverables — running stack (one command), CI-green test suite, latency-vs-SLO table, failure matrix, tagged release
## ⚠️ Common Mistakes — rebuilding instead of reusing Week K's template (the template IS the curriculum working); SLO checked once on a warm cache.
