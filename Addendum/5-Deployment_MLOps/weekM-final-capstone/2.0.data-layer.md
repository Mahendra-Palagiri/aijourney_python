# Week M — Day 2: The Data Layer — Ingestion, Storage, Contracts, Versions
> Pairs with `2.1.data-layer.py`

## 📚 Today
1. Ingestion: pull/scrape/download the real data; idempotent ingest script (K-4) writing to SQLite (J-3)
   with an append-only raw table + processed views (J-4) — simulate temporal arrival if the source is static
   (split by date; "replay" it — this makes Days 5–7 real).
2. Contracts at the door: pandera/pydantic schema (L-3) validating every ingest; quarantine table for
   rejects (don't drop — count and inspect; Week 4's missingness instincts at system scale).
3. DVC-version the training snapshots (L-3); document the source (data README — provenance, license, quirks).
4. EDA with the Week-2/4 toolkit + the audit with Week I-1's (dedupe, balance, label spot-check) — committed
   as the data report.

## 🛠️ Deliverables — ingest.py (idempotent, tested), schema contract + quarantine, versioned snapshot v1, data report
## ⚠️ Common Mistakes — static-data laziness (no temporal replay = Days 5–7 become theater); contracts written after EDA instead of refined by it; license unchecked.
