# Week M — Day 3: Modeling — Tracked, Fair, Boring (in the best way)
> Pairs with `3.1.modeling-tracked.py`

## 📚 Today
1. The harness first (Week I-2 law), then baselines, then the shuffled-label leak test — numbers on the board
   before any cleverness.
2. Candidates per pre-registration (2–3 families from Modules 2/4 with mechanistic reasons), every run
   MLflow-tracked with data-hash tags (L-1/L-3) — the league table generated from the tracker (L-1's
   best_run_report, reused).
3. Error reading + ONE disciplined iteration cycle (Week I-3; the experiment log is the tracker now).
4. Finalists frozen by protocol; holdout ceremony (Week I-4, with bootstrap CI); robustness battery scaled
   to fit the week (slices + calibration minimum).
5. Register the chosen model with full lineage (L-2); promotion criteria file written (the gate Day 5 will use).

## 🛠️ Deliverables — tracked runs + league table, holdout w/ CI, registered model v1 + promotion criteria
## ⚠️ Common Mistakes — modeling expanding to fill the week (it gets ONE day by design — the system is the point; your Module-2/4 skills make one day enough); untracked "quick checks" (no run ID, didn't happen).
