# Week M — Day 5: Automation — The Retraining Pipeline, For Real
> Pairs with `5.1.automation-retraining.py`

## 📚 Today
1. Instantiate the Week-L DAG for THIS system: ingest → validate → features → train (tracked) → evaluate →
   register → gate → (manual approve) promote → serving picks up alias. Minimal runner or Prefect — your
   L-4 verdict decides; defend the choice in one paragraph.
2. Schedule it against the temporal replay (Day 2's arrangement pays off): "weeks" of data arrive, pipeline
   retrains on schedule, gate evaluates candidates against production.
3. Idempotency + partial-failure drills on the real DAG (K-4/L-4 standards).
4. Run TWO full cycles and keep the artifacts: one where the candidate loses (gate holds production) and one
   where it wins (promotion + serving flip, no redeploy — L-2's demo, now in YOUR system).
5. Rollback drill timed and logged (the runbook number, refreshed).

## 🛠️ Deliverables — the DAG (tested), 2 logged cycles (one rejection, one promotion), drill logs, updated runbooks
## ⚠️ Common Mistakes — auto-promotion sneaking in (the manual gate is a FEATURE at this scale); cycles run but artifacts not kept (the defense needs the evidence).
