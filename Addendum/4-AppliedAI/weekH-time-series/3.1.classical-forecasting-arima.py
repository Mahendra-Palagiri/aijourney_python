"""
Week H — Day 3: Classical Forecasting — Baselines, Exponential Smoothing, ARIMA
===============================================================================

Practice placeholder — pairs with `3.0.classical-forecasting-arima.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Baselines + their rolling-origin scores on AirPassengers (the number to beat, established).
  2. Simple exponential smoothing from scratch (5 lines); α sweep {0.1, 0.5, 0.9}: plot the memory-length trade-off
  3. Manual AR(p): build the lagged design matrix with shift(); fit LinearRegression (Week 6's estimator, literally
  4. Full ARIMA via statsmodels: orders from your Day-2 analysis; residual whiteness check; forecast + intervals pl
  5. The league table: baselines vs SES vs Holt-Winters vs ARIMA, rolling-origin MAE at h ∈ {1, 6, 12}.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 3.0.classical-forecasting-arima.md")
