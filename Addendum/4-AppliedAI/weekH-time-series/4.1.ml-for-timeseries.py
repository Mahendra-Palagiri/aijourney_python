"""
Week H — Day 4: ML for Time Series — Feature Engineering Meets Honest Splits
============================================================================

Practice placeholder — pairs with `4.0.ml-for-timeseries.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. `make_ts_features(series, lags, windows)` — the careful function; unit-test it for leakage (assert feature
  2. LGBM on features, rolling-origin backtest, joined to Day-3's league table (one table to rule the week).
  3. The leakage A/B: identical setup but rolling features WITHOUT the shift — watch the fake score; write the
  4. Recursive vs direct multi-step comparison plot (error vs horizon, both strategies).
  5. Importance plot + sanity audit paragraph.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 4.0.ml-for-timeseries.md")
