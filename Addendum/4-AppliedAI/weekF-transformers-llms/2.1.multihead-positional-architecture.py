"""
Week F — Day 2: Multi-Head Attention, Positional Encoding & the Block
=====================================================================

Practice placeholder — pairs with `2.0.multihead-positional-architecture.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. MultiHeadAttention module from scratch (using Day-1's core): the reshape gymnastics, asserted shapes at every 
  2. Sinusoidal positional encodings: implement, plot the classic stripe heatmap, then PROVE the point — shuffle in
  3. The full TransformerBlock module (pre-norm variant); forward a batch; match param count to your hand calculati
  4. Head-specialization peek: 2-layer model on toy data; heatmap each head on one sentence; caption what each seem
  5. Stack 2 blocks + classifier head → tiny text classifier; train on Week-E data with the Week-10 loop. It should

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 2.0.multihead-positional-architecture.md")
