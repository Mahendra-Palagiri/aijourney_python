"""
Week F — Day 3: Build & Train a Tiny GPT — Language Modeling from Scratch
=========================================================================

Practice placeholder — pairs with `3.0.tiny-gpt-training.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Char-tokenizer + windowed Dataset/DataLoader (Week-10 muscle memory, new data shape).
  2. TinyGPT module (d=128, 4 heads, 4 blocks, context 128 — fits CPU); param count printed and hand-verified.
  3. Training loop with val split, loss curves, and generated 200-char samples at epochs 0/1/5/end — commit the
  4. Sampling functions; the same prompt at temperature 0.2/0.8/1.5 — three outputs, one caption each.
  5. Bonus honesty check: bits-per-character vs a unigram baseline (your model must beat frequency-guessing decisiv

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 3.0.tiny-gpt-training.md")
