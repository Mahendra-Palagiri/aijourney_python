# Week F — Day 7: Mini-Project & Defense — The NLP Capstone Table
> Pairs with `7.1.transformers-miniproject.py`

## 📚 Topics
Consolidation of Weeks E+F: one NEW task end-to-end, full-stack choice justified, defense written;
the "five generations" retrospective.

## 🎯 Learning Goals
1. On a FRESH dataset (not 20newsgroups — pick: sentiment, spam, support-ticket routing, or your own scraped set),
   run the pre-registered protocol with the three SENSIBLE candidates (don't re-run all five generations —
   choose with reasons: e.g., TF-IDF baseline, frozen-features, fine-tuned).
2. Justify the deployment pick with cost columns (latency per 1k docs, model size, fine-tune cost) — not just score.
3. Write `weekF_defense.md`: framing, protocol, table, error reading (10 examples), interpretability attempt
   (attention visualization caveats — attention ≠ explanation; cite your own Day-2 head inspection humility),
   risks (drift, prompt-injection-if-RAG, bias from pretraining corpus — Week B-5 lens on language).
4. Write the two-week retrospective: the five-generation story (counts → static vectors → recurrence → attention →
   pretrained scale) in YOUR words, one page, no jargon unexplained. If you can write this page, the module worked.

## 🛠️ Deliverables
- [ ] Pre-registration block · - [ ] Three-candidate table (CV ± std, holdout once) · - [ ] Cost columns
- [ ] Error-reading section · - [ ] `weekF_defense.md` · - [ ] The five-generation retrospective page

## ⚠️ Common Mistakes
New dataset chosen too easy (ceiling effects flatten the comparison) · skipping the retrospective (it IS the point) ·
defending the fine-tuned model "because transformers" (the table argues, not the fashion).

## 🔗 Connects To
Week G starts the same arc on images — predictions again: write 3 lines on what will transfer.

## 📖 References — every defense you've written; this one should be the best yet
