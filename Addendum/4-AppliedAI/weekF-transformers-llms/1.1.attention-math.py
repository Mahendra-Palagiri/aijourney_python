"""
Week F — Day 1: Attention from Scratch — Queries, Keys, Values
==============================================================

Practice placeholder — pairs with `1.0.attention-math.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. The paper exercise verified: NumPy attention (≤15 lines) reproducing your hand computation exactly.
  2. Attention-weight heatmap for a toy sentence; verify rows sum to 1; read one row aloud ("token 3 attends 70% to
  3. The √d_k experiment: distribution of QKᵀ entries with/without scaling at d ∈ {4, 64, 512}; softmax entropy col
  4. Causal mask: implement the upper-triangular −inf mask; show position t cannot see t+1 (this single line is wha
  5. Match torch.nn.functional.scaled_dot_product_attention on the same inputs.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 1.0.attention-math.md")
