"""
Week F — Day 4: The Pretrained World — Hugging Face, BERT-style vs GPT-style
============================================================================

Practice placeholder — pairs with `4.0.huggingface-pretrained.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Tokenizer autopsy: run BPE/WordPiece on 5 tricky strings (long words, typos, emoji, code); print the subword
  2. The polysemy experiment (bank/bank/riverbank cosines, distilbert hidden states) + a UMAP of 50 contextualized
  3. Frozen-feature classifier on the Week-E dataset; append to the league table (it should embarrass your LSTM; sa
  4. Probe layers: same classifier on hidden states from layers 1/6/12 — plot score vs layer (representation depth,
  5. `pipeline()` tour (sentiment, NER, summarization) — 10 minutes of fun, then back to mechanics.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 4.0.huggingface-pretrained.md")
