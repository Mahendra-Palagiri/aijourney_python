# Week I — Day 4: Final Evaluation, Robustness & Interpretation
> Pairs with `4.1.final-eval-robustness.py`

## 📚 Today
1. The single-use holdout, executed with ceremony: chosen model, refit on train+val, one run, the number is
   the number (bootstrap its CI — qb-week12 challenge 1).
2. Robustness battery: slices (Week G-4), corruptions/perturbations fit to domain (typos for text, blur for
   images, regime shifts for ts), calibration curve + fix if the decision uses probabilities (Week B-4).
3. Interpretation: SHAP/Grad-CAM/attention-with-caveats as fits the domain — explain 5 right and 5 wrong
   predictions; one "right answer, wrong evidence" hunt.
4. Fairness/segment audit where humans are in the data (Week B-5 metrics per group + model card section).

## 🛠️ Build in the paired .py
Holdout ceremony cell (literally one block, run once, output committed) + robustness battery + interpretation
gallery; all through the harness.

## ⚠️ Common Mistakes
Holdout re-run "just to check" (the number moved? you now have two numbers and zero honesty) · robustness as
afterthought prose instead of measured table · interpretation only on successes.
