> # ⚠️ DRAFT — DO NOT STUDY AS WRITTEN
> Written June 2026, BEFORE the main course Phase 5 (Weeks 13–16: NLP, CV, time series, capstone) was built.
> **After the main course is finished:** compare each week below against what the main course actually covered,
> delete duplicates, keep only the delta (expected keepers: Week F transformers/LLMs, Week G-2 DL toolkit,
> Week E-5/6 RNN→attention bridge — verify, don't assume). Track the revision in ../CHECKLIST.md.

# 🚀 Module 4 — Applied AI (the upgraded Phase 5)

The main README planned NLP / CV / time series (weeks 13–16). This module delivers that plan upgraded for
2026: a full transformers/LLM week (the analysis's biggest gap), transfer learning as the unifying
macro-theme, and the missing DL training toolkit. **33 lessons, 5 weeks — the addendum's core.**

| Week | Theme | Days | Capstone artifact |
|---|---|---|---|
| E | NLP foundations: tokens → TF-IDF → embeddings → RNN/LSTM | 7 | `weekE_defense.md` (3-generation table) |
| F | Transformers & LLMs: attention math → tiny GPT → HF → fine-tune → RAG | 7 | `weekF_defense.md` (5-generation table) |
| G | Computer vision: augmentation → DL toolkit → transfer learning → eval kit → scale → landscape | 7 | `weekG_defense.md` + model card |
| H | Time series: decomposition → ACF → ARIMA → ML features → LSTM → multi-series | 7 | `weekH_defense.md` (league table + intervals) |
| I | Applied capstone (pick your domain) | 5 | `weekI_defense.md` + model card + saved artifact |

## The macro-theme to watch for
**Representation transfer**: eigen-digits (C-5) → GloVe (E-4) → pretrained BERT (F-4/5) → ImageNet ResNet (G-3)
→ CLIP (G-6). The same idea, five costumes. If you can write Week F-7's "five generations" page and Week I's
retrospective fluently, the module worked.

## Standing rules
- Every week: pre-registration before scores; error reading before iteration; defense before moving on.
- League tables accumulate WITHIN a week — keep harnesses reusable.
- New packages as encountered: `gensim nltk transformers datasets sentence-transformers umap-learn` (E–F),
  `(open_clip)` (G), `prophet lightgbm` (H). Log them in addendum-requirements.txt.

## Status
- [ ] Week E · - [ ] Week F · - [ ] Week G · - [ ] Week H · - [ ] Week I → flip ✅ in Addendum README
