"""
Week E — Day 1: Text as Data — Preprocessing & Tokenization
===========================================================

Practice placeholder — pairs with `1.0.text-preprocessing-tokenization.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. `SimpleTokenizer` class: fit(corpus) builds vocab, encode/decode with UNK + PAD specials; round-trip test.
  2. Zipf's law plot: token frequency vs rank (log-log) on a real corpus (sklearn `fetch_20newsgroups`, 2 categorie
  3. Vocabulary-size experiment: coverage % vs vocab size (top-k words); find the k covering 95%.
  4. Mini-BPE: implement the merge loop on a toy corpus (20 lines); show "unhappiness" → ["un", "happi", "ness"]-st
  5. Stemming vs lemmatization comparison table on 20 tricky words (use nltk; note where stemming mangles).

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 1.0.text-preprocessing-tokenization.md")
