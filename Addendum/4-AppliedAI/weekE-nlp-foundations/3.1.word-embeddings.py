"""
Week E — Day 3: Word Embeddings — Meaning as Geometry
=====================================================

Practice placeholder — pairs with `3.0.word-embeddings.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Train word2vec on 20newsgroups (gensim, ~3 lines) — then IGNORE the magic and inspect: vocab, vector shapes, r
  2. `most_similar` by hand: cosine similarity over the whole matrix with YOUR Week-D function (no gensim helper) —
  3. The analogy function from scratch (vector arithmetic + nearest-neighbor excluding inputs); test 10 analogies, 
  4. UMAP the top-300 words (Week C-6 discipline: neighborhoods only!); annotate 3 tight clusters you can name.
  5. Bias probe: cosine(profession, "he") − cosine(profession, "she") for 20 professions; bar chart; one-paragraph 

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 3.0.word-embeddings.md")
