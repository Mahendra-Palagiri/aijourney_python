"""
Week E — Day 2: Bag-of-Words, TF-IDF & Your First Text Classifier
=================================================================

Practice placeholder — pairs with `2.0.bow-tfidf-classification.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. CountVectorizer from scratch (use Day-1 tokenizer) on a 5-doc toy corpus; verify against sklearn's.
  2. TF-IDF by hand for one (doc, term); match sklearn (mind its smoothing variant — read the docs note).
  3. Full pipeline on 20newsgroups (4 categories): Tfidf → LogisticRegression; CV mean ± std; confusion matrix; hol
  4. N-gram experiment: unigrams vs uni+bigrams — score change vs feature-count explosion (the trade-off table).
  5. Top-15 coefficients per class, printed and annotated by hand; find one suspicious feature (leaked header/foote

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 2.0.bow-tfidf-classification.md")
