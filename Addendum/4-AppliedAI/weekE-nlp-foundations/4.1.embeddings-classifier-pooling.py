"""
Week E — Day 4: From Word Vectors to Document Vectors — Pooling & Pretrained Embeddings
=======================================================================================

Practice placeholder — pairs with `4.0.embeddings-classifier-pooling.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. GloVe loader (100-d) + coverage report: % of your vocab found; what's missing (typos, rare words, names)?
  2. Three pooling strategies as sklearn-compatible transformers (fit/transform — reuse your Week-4 custom-transfor
  3. The bake-off table: 3 representations × same classifier, same folds, mean ± std; bold the winner per dataset s
  4. Failure inspection: 5 documents TF-IDF gets right and embeddings get wrong; read them; name the pattern (usual
  5. The order-blindness demo: "dog bites man" vs "man bites dog" → identical mean-pooled vectors. Print both. Star

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 4.0.embeddings-classifier-pooling.md")
