"""
Week G — Day 1: Real Image Data & Augmentation
==============================================

Practice placeholder — pairs with `1.0.image-data-augmentation.md` in this folder.

HOW TO USE (when you reach this lesson):
  1. Read the paired markdown FIRST (topics, learning goals, core mechanics).
  2. Work through the "Build in the paired .py" exercises below, in order.
  3. Keep this header docstring; append a short "what I learned / what surprised me"
     note at the bottom when done (the main repo's best habit).

EXERCISES (from the paired markdown):
  1. Stats computation + Normalize; show a normalized batch's per-channel mean≈0, std≈1.
  2. Augmentation gallery: one image × 16 augmented variants in a grid (the visceral "free data" picture).
  3. Week-12 CNN retrained on CIFAR-10 with vs without augmentation: two val curves, one conclusion (gap = variance
  4. The label-destroying demo: vertical-flip a "6"; train with it; watch 6/9 confusion in the matrix — augmentatio
  5. Reusable `make_loaders(augment: bool)` factory for the rest of the week.

Status: NOT STARTED
"""

# =====================================================================
# Exercise 1
# =====================================================================
# TODO


if __name__ == "__main__":
    print("Lesson placeholder — see paired markdown: 1.0.image-data-augmentation.md")
