

> [🔼 README](../../../README.md)

# Week 11 — Deep Learning Foundations: Convolutional Neural Networks \(CNNs\) 🖼️

## Day-by-Day Breakdown

**Goal (by end of Week 11):**

- Understand why **Convolutional Neural Networks \(CNNs\)** are useful for image and grid-like data
- Understand why fully connected networks are inefficient for raw images
- Learn how image tensors are represented in PyTorch using batches, channels, height, and width
- Understand the intuition behind **convolution**, **filters/kernels**, and **feature maps**
- Understand the basic math of convolution using sliding windows and dot products
- Learn how stride and padding affect output shapes
- Understand the role of pooling layers such as max pooling and average pooling
- Build a simple CNN architecture using PyTorch
- Train and evaluate a small CNN-style workflow
- Recognize common CNN bugs such as channel mismatch, flatten-size mismatch, and incorrect tensor shape

## Week 11 — Day 1 🔜

- ***Focus: Why CNNs Exist + Limits of Fully Connected Networks for Images***
- Understand why image data is different from regular tabular data.
- Review how a fully connected network treats input features.
- See why flattening images too early can lose spatial structure.
- Understand why images contain local patterns such as:
  - edges
  - corners
  - textures
  - shapes
- Learn why CNNs use local connections instead of connecting every pixel to every neuron.
- Build the mental model that CNNs are designed to learn spatial patterns efficiently.

✅ **Outcome:** We understand why CNNs are needed and why ordinary fully connected layers are not ideal as the first tool for raw image data.

**Checkpoint Questions:**

- Why is image data different from ordinary tabular data?
- What information can be lost when an image is flattened too early?
- Why are local patterns important in images?


## Week 11 — Day 2 🔜

- ***Focus: Image Tensors and Shape Conventions***
- Learn how images are represented numerically.
- Understand grayscale image shape vs RGB image shape.
- Learn PyTorch image tensor convention:

```text
[batch_size, channels, height, width]
```

- Compare common image shape conventions:
  - `[height, width]`
  - `[height, width, channels]`
  - `[channels, height, width]`
  - `[batch_size, channels, height, width]`
- Understand what channels mean:
  - grayscale = 1 channel
  - RGB = 3 channels
- Practice reading image tensor shapes.
- Connect image tensor shapes to CNN input layers.

✅ **Outcome:** We can read and explain image tensor shapes without confusing batch size, channels, height, and width.

**Checkpoint Questions:**

- What does `[batch_size, channels, height, width]` mean?
- Why does an RGB image usually have 3 channels?
- What is the difference between image height/width and feature count?


## Week 11 — Day 3 🔜

- ***Focus: Convolution Intuition — Filters, Kernels, and Feature Maps***
- Understand what a convolution filter/kernel is.
- Learn how a small filter slides across an image.
- Understand that each filter detects a pattern.
- Build intuition for filters that detect:
  - vertical edges
  - horizontal edges
  - simple textures
  - local shapes
- Understand what a feature map is.
- Learn that one filter produces one feature map.
- Understand that multiple filters produce multiple output channels.
- Connect convolution to the idea of learned pattern detectors.

✅ **Outcome:** We understand convolution as a sliding pattern detector that produces feature maps.

**Checkpoint Questions:**

- What is a convolution filter?
- What is a feature map?
- Why might a CNN learn multiple filters in one convolution layer?


## Week 11 — Day 4 🔜

- ***Focus: Convolution Math — Sliding Windows, Dot Products, Stride, and Padding***
- Work through a small numeric convolution example by hand.
- Understand the sliding window operation.
- Learn that convolution uses local dot products between:
  - image patch
  - filter/kernel
- Understand how stride controls movement of the filter.
- Understand how padding controls border handling.
- Learn the output-size intuition:
  - larger kernel can reduce output size
  - stride greater than 1 reduces output size faster
  - padding can preserve spatial size
- Connect convolution math to PyTorch `nn.Conv2d`.

✅ **Outcome:** We understand the basic math behind convolution and how stride/padding affect output dimensions.

**Checkpoint Questions:**

- What happens when stride increases?
- Why does padding help preserve spatial dimensions?
- How is convolution related to dot products?


## Week 11 — Day 5 🔜

- ***Focus: Pooling, Flattening, and CNN Architecture Flow***
- Understand why CNNs often use pooling layers.
- Learn the difference between:
  - max pooling
  - average pooling
- Understand how pooling reduces spatial dimensions.
- Learn the common CNN flow:

```text
image → convolution → activation → pooling → convolution → activation → pooling → flatten → classifier
```

- Understand why flattening usually happens after convolution/pooling layers, not before.
- Learn how CNN feature maps eventually become a vector for classification or regression.
- Connect CNN architecture to the Week 9 idea of layered representations.

✅ **Outcome:** We understand how convolution, activation, pooling, flattening, and classifier layers fit together in a CNN.

**Checkpoint Questions:**

- What does max pooling do?
- Why do we flatten after convolution/pooling layers?
- What is the role of the final classifier layer?


## Week 11 — Day 6 🔜

- ***Focus: Build a First Small CNN in PyTorch***
- Build a simple CNN using PyTorch modules:
  - `nn.Conv2d`
  - `nn.ReLU`
  - `nn.MaxPool2d`
  - `nn.Flatten`
  - `nn.Linear`
- Understand `in_channels` and `out_channels`.
- Connect input image shape to the first convolution layer.
- Trace shape changes through the model.
- Run a forward pass with a small dummy image batch.
- Inspect output shape.
- Understand where common shape errors happen.

✅ **Outcome:** We can define a small CNN in PyTorch and explain how data flows through it.

**Checkpoint Questions:**

- What do `in_channels` and `out_channels` mean in `nn.Conv2d`?
- Why do flatten-size errors happen?
- How can we inspect shape changes during a forward pass?


## Week 11 — Day 7 🔜

- ***Capstone: Train and Evaluate a Small CNN Workflow***
- Build a small end-to-end CNN training workflow:
  1) create or load small image-style tensors
  2) create dataset and dataloader
  3) define a CNN model
  4) choose loss and optimizer
  5) train over epochs and batches
  6) evaluate with `model.eval()` and `torch.no_grad()`
  7) inspect predictions, loss, and shape flow
- Keep the example small so the CNN mechanics remain visible.
- Focus on understanding shape flow and model structure rather than chasing performance.
- Add a short Week 11 retrospective.

✅ **Outcome:** We finish Week 11 with a small CNN workflow that connects image tensors, convolution layers, pooling, flattening, loss, optimizer, training, and evaluation.

---

## Week 11 --> Explained in Simple Words

- ***Week 11 is where we learn how neural networks work with images and grid-like data using Convolutional Neural Networks \(CNNs\).***

- ***What we’ll do \(in simple words\):***
  - Learn why images need a different model structure than ordinary tables.
  - Learn how images are stored as tensors.
  - Learn how filters slide over images to detect local patterns.
  - Learn how convolution produces feature maps.
  - Learn how pooling reduces image size while keeping important signals.
  - Build a small CNN in PyTorch.
  - Train and evaluate a tiny CNN workflow.

- ***Why we’re doing it:***
  - Because images have spatial structure.
  - Nearby pixels matter together.
  - CNNs are designed to learn local visual patterns more efficiently than fully connected layers.

- ***How it helps our AI journey:***
  - CNNs are foundational for computer vision.
  - Understanding CNNs helps us read image-model code, debug tensor-shape errors, and prepare for more advanced architectures later.
  - This week strengthens our ability to connect math, tensors, and PyTorch model structure.

---

### Core Libraries and Setup

```bash
source .venv/bin/activate
pip install -U pip
pip install torch torchvision torchaudio numpy matplotlib scikit-learn
pip freeze > requirements.txt
```

---

## Key Skills

### Image Tensor Fundamentals
- Image tensors and shape conventions
- Batch size, channels, height, and width
- Grayscale vs RGB images
- PyTorch convention: `[N, C, H, W]`

### Convolution Mechanics
- Filters/kernels
- Sliding windows
- Dot products over local patches
- Feature maps
- Stride and padding

### CNN Architecture Basics
- `nn.Conv2d`
- `nn.ReLU`
- `nn.MaxPool2d`
- `nn.Flatten`
- `nn.Linear`
- Shape flow through CNN layers

### Training and Evaluation
- CNN training loop
- Loss and optimizer usage
- Evaluation with `model.eval()` and `torch.no_grad()`
- Debugging common CNN shape errors

---

### Examples

#### Example 1 — Create a Dummy Image Batch

```python
import torch

# 4 grayscale images, 1 channel, 28x28 pixels
images = torch.randn(4, 1, 28, 28)

print(images.shape)
```

Expected shape:

```text
torch.Size([4, 1, 28, 28])
```

Meaning:

```text
4 images
1 channel
28 height
28 width
```

#### Example 2 — A Simple Convolution Layer

```python
import torch
from torch import nn

conv = nn.Conv2d(in_channels=1, out_channels=8, kernel_size=3)

images = torch.randn(4, 1, 28, 28)
outputs = conv(images)

print(outputs.shape)
```

This creates 8 feature maps for each input image.

#### Example 3 — A Small CNN Skeleton

```python
import torch
from torch import nn

class TinyCNN(nn.Module):
    def __init__(self):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(1, 8, kernel_size=3, padding=1),
            nn.ReLU(),
            nn.MaxPool2d(kernel_size=2),
            nn.Conv2d(8, 16, kernel_size=3, padding=1),
            nn.ReLU(),
            nn.MaxPool2d(kernel_size=2)
        )

        self.classifier = nn.Sequential(
            nn.Flatten(),
            nn.Linear(16 * 7 * 7, 10)
        )

    def forward(self, x):
        x = self.features(x)
        x = self.classifier(x)
        return x
```

#### Example 4 — CNN Forward Pass Shape Check

```python
model = TinyCNN()
images = torch.randn(4, 1, 28, 28)
outputs = model(images)

print(outputs.shape)
```

Expected output shape:

```text
torch.Size([4, 10])
```

Meaning:

```text
4 images
10 output scores per image
```