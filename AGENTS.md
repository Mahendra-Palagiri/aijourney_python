# AI Learning Project Instructions

## Canonical Project Location

- The canonical project named **AI** is located at `/Users/mahendrapalagiri/Documents/GitHub/aijourney_python`.
- Treat that directory as the target for project files. Do not substitute a Codex worktree path when the user refers to the AI project.
- Before writing a project file, confirm that the resolved target begins with `/Users/mahendrapalagiri/Documents/GitHub/aijourney_python/`.

## Lesson Presentation

- Present each complete lesson inside one continuous `.md` document display block.
- Do not split a lesson across ordinary chat sections.
- Illustrations complement the complete theory; they never replace, shorten, or remove written content.
- Use renderer-safe Unicode equations inside document display blocks, such as `÷`, `×`, `−`, `≈`, `μ`, and `σ`. Do not use LaTeX delimiters that appear as source text.

## File Population

- Do not create or update a learning file until the user explicitly says `Populate the file`.
- Confirm the exact target path before writing.
- Preserve the approved chat content when populating a lesson file.
- Markdown lesson files include practice guidance and concept-check answer keys.
- Complete executable programs belong in separate `.py` files and require an explicit request.
